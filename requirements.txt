# Core API / schema
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6

# Crawling / parsing
requests>=2.31
beautifulsoup4>=4.12
playwright>=1.40

# Retrieval / tokenization / normalization
jieba>=0.42
opencc-python-reimplemented>=0.1.7

# Test
pytest>=8.0

# Optional Day 63B dense runtime (bge-m3 path)
# Install only when running Day 63B dense build/eval locally:
# FlagEmbedding>=1.2
# torch>=2.1
# transformers>=4.37
